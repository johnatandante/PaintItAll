<UserControl x:Class="PaintItAll.Components.ChooseColorItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="56" d:DesignWidth="480">
    
    <Grid Width="Auto" Height="Auto">
        <StackPanel x:Name="LayoutRoot" Width="Auto" Orientation="Horizontal" MinHeight="48">
            <TextBlock Text="{Binding ColorComponent}" Width="50" HorizontalAlignment="Center" />
            <Slider Orientation="Horizontal" Width="256" Minimum="0" Maximum="255" Value="{Binding ColorComponentValue}" />
            <TextBox Text="{Binding ColorComponentValue}" Height="24" Width="100" HorizontalAlignment="Center"  />
        </StackPanel>
    </Grid>
</UserControl>
